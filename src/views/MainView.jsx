import React from "react";
import Form from "../components/Form/Form";
import styles from "../style";
const MainView = () => {
  return (
    <div className={`${styles.paddingXClass} `}>
      <Form />
    </div>
  );
};

export default MainView;
